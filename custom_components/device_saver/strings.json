{
  "title": "Device Saver",
  "config": {
    "step": {
      "user": {
        "title": "Device Saver",
        "description": "All devices are monitored automatically. Battery devices use the slow timeout, all others use the critical timeout. Individual devices can be excluded.",
        "data": {
          "devices_excluded": "Excluded devices",
          "timeout_critical_minutes": "Critical timeout (minutes)",
          "timeout_slow_minutes": "Slow timeout (minutes)",
          "notify_service": "Optional notify service (e.g. notify.mobile_app_...)",
          "notify_recovered": "Notify also on recovery"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Device Saver Options",
        "menu_options": {
          "settings": "Settings",
          "add_gate": "Add power gate",
          "remove_gate": "Remove power gate"
        }
      },
      "settings": {
        "title": "Device Saver Options",
        "data": {
          "devices_excluded": "Excluded devices",
          "timeout_critical_minutes": "Critical timeout (minutes)",
          "timeout_slow_minutes": "Slow timeout (minutes)",
          "notify_service": "Optional notify service (e.g. notify.mobile_app_...)",
          "notify_recovered": "Notify also on recovery",
          "ignored_integrations": "Ignored integrations (devices belonging only to these are skipped)",
          "ignored_platforms": "Ignored platforms (their entities don't count for health)"
        }
      },
      "add_gate": {
        "title": "Add power gate",
        "description": "While the gate entity is off, the device counts as deliberately unpowered instead of down.",
        "data": {
          "device": "Device",
          "gate_entity": "Gate entity (switch)"
        }
      },
      "remove_gate": {
        "title": "Remove power gate",
        "data": {
          "remove": "Gates to remove"
        }
      }
    },
    "abort": {
      "no_gates": "No power gates configured."
    }
  }
}
